# tennis1v1

Minimalist browser tennis game — play against AI or a real opponent online.

## Stack

- **Client**: Vite + HTML5 Canvas (vanilla JS)
- **Server**: Node.js + WebSocket (`ws`)

## Development

```bash
# Client
cd client && npm install && npm run dev

# Server
cd server && npm install && node index.js
```
